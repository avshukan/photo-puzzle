# AI Context (paste this + prompt.txt into a new chat)

You are reviewing my pet project repository: **photo-puzzle** (React + TS, Vite).
The project is a 15-puzzle game. Current release: **v0.1.0** (MVP done).

## Current state

- MVP is finished and deployed.
- Next step: plan **Iteration 1** using the backlog (`docs/08-backlog.md`).

We work using **Incremental Delivery**:

- plan work in iterations
- implement vertical slices
- each iteration ends with a release (tag + deploy)

Rules:

- Prefer repository docs as the source of truth.
- Keep answers short and practical.
- Ask only for files you really need.
